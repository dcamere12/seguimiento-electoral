
/*global $*/
'use strict';

var home = {
    cache: function () {
        //this.$item = $('.item');
    },

    render: function () {
        //home.$item.each(function (i) {
           
        //});
    },

    run: function () {
        //$(window).on('ready resize', home.render);
    },

    init: function () {
        //this.cache();
        //this.render();
        //this.run();
    }
};

$(document).ready(function () {
    home.init();
});


